doctype
html
  head
    meta(charset="utf-8")
    meta(name="viewport" content="width=device-width, initial-scale=1")
    title #{title}
    <link rel="shortcut  icon" type="image/x-icon" href="/images/favicon.ico" media="screen"  />
    include ./includes/head
  body
    .top_bar
      p.top_bar_p 欢迎来到内农大商层 &nbsp;&nbsp;&nbsp;&nbsp;+15754885016
      if user
        .login
            span(style="display:inline;color:white;") 欢迎您, #{user.name}
            span(style="display:inline;color:white;") &nbsp;|&nbsp;
            a.nvabar-link(href="/logout" style="display:inline;color:white;" target="_blank") 登出
      else
        .login
          a.nvabar-link(href="/signup" style="display:inline;color:white;" target="_blank") 注册
          span &nbsp;|&nbsp;
          a.nvabar-link(href="/signin" style="display:inline;color:white;" target="_blank") 登录
    block content